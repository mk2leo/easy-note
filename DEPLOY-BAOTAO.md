# 🚀 網上記事簿 — 寶塔面板部署教學（Rocky Linux）

呢份教學教你用 **寶塔面板（aaPanel / BT Panel）** 將「網上記事簿」部署到你嘅 Rocky Linux 伺服器。全程喺寶塔網頁介面操作，唔使敲命令。

> 如果你用緊傳統 SSH + systemd 方法，睇返 `DEPLOY.md`。

---

## 一、準備事項

- 一部已裝寶塔面板嘅 Rocky Linux 伺服器
- 可以登入寶塔面板（`http://你的伺服器IP:8888`）
- 一個域名（可選，推薦，因為寶塔最擅長管域名 + HTTPS）

---

## 二、喺寶塔安裝 Node.js

寶塔預設未必有 Node.js，需要用佢內置嘅 **Node 版本管理器** 安裝。

### 步驟：

1. 登入寶塔面板
2. 左邊菜單揀「**軟件商店**」
3. 搜尋「**Node**」→ 搵到「**Node.js 版本管理器**」→ 撳「**安裝**」
   - 佢會一併幫你裝好 Nginx 等（如未有）
4. 安裝完成後，撳入「Node.js 版本管理器」
5. 喺「版本管理」頁 → 揀 **Node.js v22.x**（**必須 v22 或以上**，因為記事用 SQLite 需要 v22 內置模組）→ 撳「**安裝**」
6. 裝好之後，撳「**設置**」/「**環境變量**」確保 `node` 同 `npm` 命令可用

> **點解要 v22+？** 記事簿而家用 SQLite 資料庫（`node:sqlite` 內置模組，v22 先有）。如果寶塔只得 v18/v20，記事簿都會照行，但會自動改用 JSON 文件儲存（功能一樣，都係存伺服器硬盤），只係唔係真正 SQLite。
> 想用真 SQLite，寶塔 Node 版本管理器裝 v22.x 就得。

### 驗證（可選）：
寶塔提供「**終端**」功能（左上角/右邊「終端」按鈕），入面輸入：
```bash
node -v   # 應該顯示 v18.x
```

---

## 三、上傳項目到伺服器

### 方法 A：寶塔文件管理（推薦，可視化）

1. 寶塔左邊揀「**文件**」
2. 進入你想要嘅目錄，例如 `/www/wwwroot`
3. 撳「**上傳**」→ 將 `notesbook.zip` 上傳上去
4. 揀中 `notesbook.zip` → 撳右鍵（或上方工具列）→「**解壓**」
5. 解壓完會得到 `notesbook/` 資料夾

### 方法 B：寶塔終端 + 命令
如果你已經用 `scp` 上傳咗，或者想用命令：
```bash
cd /www/wwwroot
unzip notesbook.zip
```

---

## 四、用寶塔「網站」功能部署（推薦做法）

寶塔最正路嘅做法係建立一個「Node 項目」網站，等佢自動管理服務。

### 方法 1：寶塔「Node 項目」（新版寶塔）

1. 左邊揀「**網站**」→ 揀「**Node 項目**」標籤頁（如果冇就揀「PHP項目」旁邊）
2. 撳「**添加 Node 項目**」
3. 填寫：
   - **項目目錄**：`/www/wwwroot/notesbook`
   - **項目名稱**：`notesbook`（自己改）
   - **啟動文件**：`server.js`
   - **端口**：`8686`
4. 撳「**提交**」
5. 寶塔會自動用 **PM2** 幫你運行，並設好開機自啟（好方便！）

### 方法 2：如果寶塔版本冇「Node 項目」選項（用反向代理）

如果搵唔到 Node 項目功能，就用「網站 + 反向代理」：

1. 左邊「**網站**」→「**添加站點**」
2. 填域名（或留空用 IP），創建一個站點
3. 進入呢個站點嘅「**設置**」→「**反向代理**」標籤
4. 撳「**添加反向代理**」：
   - **代理名稱**：`notesbook`
   - **目標 URL**：`http://127.0.0.1:8686`
   - **發送域名**：留空或填你域名
5. 保存
6. 跟住仲要確保 Node 服務喺跑（見下一步「手動啟動」）

---

## 五、啟動 Node 服務

如果上面用「Node 項目」方法，寶塔已經幫你搞掂，跳過呢步。

如果用「反向代理」方法，你需要手動啟動 Node 服務。用寶塔「**終端**」：

```bash
# 第一次啟動測試
cd /www/wwwroot/notesbook
node server.js
```
見到 `📒 網上記事簿已啟動` 就成功。

**要常駐運行**，用寶塔內置嘅 PM2 管理器：
1. 軟件商店 → 裝「**PM2 管理器**」
2. 撳入 PM2 管理器 →「**添加項目**」
3. 填：
   - **啟動目錄**：`/www/wwwroot/notesbook`
   - **啟動文件名**：`server.js`
4. 提交 → PM2 會幫你長駐 + 開機自啟

---

## 六、開放防火牆（重要）

### 寶塔面板防火牆
1. 寶塔左邊「**安全**」→「**防火牆**」
2. 撳「**放行端口**」
3. 端口填 `8686`，備註寫「記事簿」，撳「**放行**」

### 騰訊雲控制台防火牆（外部）
1. 登入騰訊雲控制台 → 輕量應用伺服器 → 你嘅伺服器 → 「**防火牆**」
2. 撳「**添加規則**」
3. 應用類型「**自定義**」→ 端口 `8686` → 協議 TCP → 來源 `0.0.0.0/0`
4. 確定

> ⚠️ **兩個防火牆都要開**，寶塔同騰訊雲都開咗先得。

---

## 七、用域名 + HTTPS（推薦）

寶塔最強項就係一鍵 SSL。

### 如果你用「Node 項目」方法：
Node 項目默認直接聽端口，域名訪問要加反向代理。

1. 「**網站**」→「**添加站點**」→ 填你域名（例如 `notes.mydomain.com`）→ 創建
2. 進入呢個站點 →「**反向代理**」→ 添加：
   - 目標 URL：`http://127.0.0.1:8686`
3. 撳「**SSL**」標籤 →「**Let's Encrypt**」→ 勾選域名 → 撳「**申請**」→ 自動配置免費 HTTPS 證書
4. 撳「**強制 HTTPS**」開關，以後訪問自動跳 HTTPS

### 完整流程示意
```
用戶瀏覽器
   │
   ▼ https://notes.mydomain.com
寶塔 Nginx (SSL終端 + 反代)
   │ proxy_pass http://127.0.0.1:8686
   ▼
Node server.js (記事簿)
```

### 上傳大小限制（重要）
記事簿要上傳圖片/文件，寶塔 Nginx 默認上傳限制太細（1MB），要調大：

1. 你個站點「**設置**」→「**配置文件**」
2. 喺 `server { ... }` 入面加一行：
```nginx
client_max_body_size 100M;
```
3. 保存 → 寶塔會自動重載 Nginx

---

## 八、數據儲存位置（重要）

- **記事內容**：存喺伺服器資料庫 `notesbook/data/notesbook.db`（SQLite，Node v22+）；舊 Node 會存 `data/notes.json`
- **分享數據**：都喺資料庫入面（`notesbook/data/`）
- **上傳圖片/文件**：`notesbook/public/uploads/`

> ✅ 記事而家**存喺伺服器**，唔係瀏覽器！所以換機、換瀏覽器、清快取都唔會唔見，多人睇到同一啲記事。

---

## 九、備份（建議定期做）

資料庫同圖片係你嘅數據，建議定期備份：
1. 寶塔左邊「**計劃任務**」→「**添加任務**」
2. 揀「**備份網站**」→ 揀 notesbook 站點
3. 設定每日/每週自動備份
4. 或者手動備份：下載 `notesbook/data/notesbook.db` 同 `notesbook/public/uploads/` 就得

---

## 十、更新項目

1. 寶塔「文件」→ 上傳新版 `notesbook.zip` → 解壓
2. 或者用「終端」：
```bash
cd /www/wwwroot
# 備份舊數據
cp -r notesbook/data notesbook_data_backup
cp -r notesbook/public/uploads notesbook_uploads_backup
# 解壓新版覆蓋
unzip -o notesbook.zip
# 還原數據
cp -r notesbook_data_backup/* notesbook/data/
cp -r notesbook_uploads_backup/* notesbook/public/uploads/
# 重啟服務
pm2 restart notesbook   # 或喺寶塔 PM2 管理器撳重啟
```

> ⚠️ **更新前一定要備份 `data/` 同 `public/uploads/`**，唔好畀新版覆蓋咗你嘅資料庫同上傳文件。

---

## 十一、常見問題

**Q: 網站打唔開？**
1. 確認 Node 服務喺跑（寶塔 PM2 管理器睇狀態）
2. 確認 8686 端口放行（寶塔防火牆 + 騰訊雲防火牆）
3. 本機測試：寶塔終端 `curl http://127.0.0.1:8686`

**Q: 上傳文件失敗？**
- 睇第七節，將 Nginx `client_max_body_size` 加到 100M

**Q: 反代 502 Bad Gateway？**
- Node 服務未啟動。開 PM2 管理器，確認 `notesbook` 項目喺運行
- 睇 Node 日誌有冇報錯

**Q: 分享連結打唔開？**
- 確認分享咗嘅筆記存在，且未取消分享
- 確認資料庫入面有分享記錄（`data/notesbook.db`）

---

祝你架設順利！有問題可以問我。😊
